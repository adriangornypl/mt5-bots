# Agents

Before any work in this workspace, read `.ai/docs/` (start with `.ai/docs/README.md`).

Do not assume EA input defaults from chat memory — read the current `input` block in the file you are changing (`Event_Scalp.mq5`, `MACD_RSI_Cross.mq5`, or `Stochastic_Trend.mq5`). Tester `.set` / `.ini` and the chart override compiled defaults until the user Resets inputs.

Human write-ups (English): `docs/README.md`. They are not the technical source of truth.
